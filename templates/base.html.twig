{% extends 'baseOut.html.twig' %}
        
            {% block nav %}
                <nav class="nav">        
                        <div>                  
                            <a class="navbtn" href="{{ path('app_medico_new') }}">Agregar Medico</a>
                            <a class="navbtn" href="{{ path('app_medico_index') }}">Consulta Medicos</a>
                            <a class="navbtn" href="{{ path('app_obstetra_new') }}">Agregar Obstetra</a>
                            <a class="navbtn" href="{{ path('app_obstetra_index') }}">Consulta Obstetra</a>
                        </div>                   
                        <div>
                            {% if app.user %}                      
                            <span class="navbtn">Usuario: {{ app.user.userIdentifier }}</span>
                            <a class="navbtn" href="{{ path('app_logout') }}">Logout<i class="fa-sharp fa-solid fa-arrow-right-from-bracket"></i></a>
                            {% endif %}
                        </div>            
                </nav>                
            {% endblock %}            
       
            

            {% block encabezado %}  
                
            {% endblock %}            
            

           